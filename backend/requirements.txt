# BrandMeld Backend Dependencies — Agentic Architecture

# Web Framework (minimal — health probes + future approval API)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6

# LLM client (NVIDIA NIM API)
httpx>=0.27.0

# Database (Supabase — used by integrations/storage.py)
supabase>=2.0.0

# JWT validation — kept for future approval API auth
PyJWT>=2.8.0

# Environment / config
python-dotenv==1.0.1

# Removed from agent MVP (add back when needed):
#   playwright>=1.40.0       — brand discovery (URL scrape path, Feature 3)
#   APScheduler>=3.10.4      — cron jobs (use Cloud Scheduler in prod)
#   slowapi>=0.1.9           — rate limiting (add back with approval API)
#   cryptography>=42.0.0     — OAuth token encryption (add back with platform posting)
